# 本地启动指南（AI Demo Hub · 接线机器人 + 电销工作台）

本平台为前后端分离全栈系统：NestJS + Prisma + PostgreSQL（缓存可无 Redis 走进程内）、React18 + Vite + AntD。
内置沙箱 NLU/ASR/TTS 模拟，**无需任何外部 AI/语音密钥即可完整体验**；真实机器人/大模型/短信通道后续在「供应商与集成」页面填入密钥即可切换。

> 最快路径：装好 Docker Desktop 后，Windows 双击「启动-Demo.bat」、macOS 双击「启动-Demo.command」，脚本会自动构建、启动并打开浏览器，无需手敲命令。

## 方式一：Docker 一键启动（推荐，最省事）

前置：安装 [Docker Desktop](https://www.docker.com/products/docker-desktop/)（Windows/macOS 均可，安装后保持运行）。

```bash
# 在本项目根目录（含 docker-compose.yml 的目录）执行
docker compose up -d --build
```

首次会构建 4 个容器（PostgreSQL、Redis、后端 API、前端 Nginx），自动完成数据库迁移、种子数据写入。
完成后浏览器打开：

- 平台地址：<http://localhost:8080>
- 演示账号（密码统一 `Aihub@123456`）：
  - `admin` 平台管理员（全权限）
  - `manager` 销售主管
  - `agent01` / `agent02` 电销坐席

常用命令：

```bash
docker compose ps          # 查看状态
docker compose logs -f api # 跟踪后端日志
docker compose down        # 停止（数据保留在数据卷）
docker compose down -v     # 停止并清空数据，恢复初始演示状态
```

端口占用时改 `docker-compose.yml` 末尾的 `8080:80`（左侧为本机端口）。

## 方式二：不用 Docker，本机 Node 直接跑

前置：Node.js 20+（建议 20/22 LTS）、本机 PostgreSQL 14+。

1. 安装依赖（项目根目录，npm workspaces 会同时装好前后端）：

```bash
npm install --ignore-scripts
cd server && npx prisma generate && cd ..
```

2. 建库并配置连接串。创建一个数据库（示例名 `ai_demo_hub`），然后设置环境变量：

```bash
# macOS/Linux
export DATABASE_URL="postgresql://用户名:密码@127.0.0.1:5432/ai_demo_hub?schema=public"
# Windows PowerShell：$env:DATABASE_URL="postgresql://用户名:密码@127.0.0.1:5432/ai_demo_hub?schema=public"
```

3. 数据库迁移 + 写入演示种子：

```bash
cd server
npx prisma migrate deploy
npx ts-node prisma/seed.ts   # 或 node dist/prisma/seed.js（需先 npm run build）
cd ..
```

4. 启动后端（终端 1，默认 3000 端口）：

```bash
npm run dev:server
# 生产方式：cd server && npm run build && NODE_ENV=development npm start
```

5. 启动前端（终端 2，默认 5173 端口，Vite 已配置 /api 代理到 3000）：

```bash
npm run dev:web
```

浏览器打开 Vite 输出的本地地址（一般是 <http://localhost:5173>），用上面的演示账号登录。

> 说明：后端默认走进程内内存缓存，不启动 Redis 也能完整演示；如需 Redis，本机起一个后设置 `REDIS_URL` 即可。
> 本地演示建议保持 `NODE_ENV=development` 与 `ALLOW_DEMO_INBOUND=true`，这样「呼入模拟器」等沙箱入口可用。

## 快速体验路线

1. 登录后进「呼入模拟器」，点「模拟来电」，用快捷话术体验机器人应答、自动留资、说「转人工」派单；
2. 回「坐席工作台」看待接派单队列，接听后在「实时通话」看实时转写与 AI 小结；
3. 依次浏览客户 CRM、外呼任务、机器人配置/意图/知识库、智能质检、合同回款、营销触达、数据报表、管理端。

## 后续接入真实机器人/通道

在「管理端 → 供应商与集成」配置语音线路（SIP/CTI 回调密钥 `CTI_WEBHOOK_SECRET`）、ASR、TTS、大模型（LLM）、短信密钥即可把内置沙箱替换为商用通道，业务代码无需改动。生产部署请使用高强度 `JWT_SECRET / JWT_REFRESH_SECRET / CREDENTIAL_KEY`，并设置 `NODE_ENV=production`。

## 常见问题

- 8080/3000/5432 端口被占用：改 compose 端口映射或停掉占用进程。
- 页面打开是登录页但登录转圈：确认后端容器/进程已就绪（`/api/v1/health/ready` 返回 `database=up`）。
- 想重置演示数据：Docker 用 `docker compose down -v && docker compose up -d --build`；本机方式删库重建后重跑迁移与种子。
