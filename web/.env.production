# 生产构建默认：同源部署（由 nginx/网关反代 /api）。
# 运行时 /config.js 优先级高于本文件，部署后改域名无需重新打包。
# 若要在「打包时」写死后端域名，取消下行注释并改为真实地址：
# VITE_API_BASE=https://api.your-domain.com/api/v1
# VITE_WS_ORIGIN=https://api.your-domain.com
